import { searchClassNameModules } from "ittai/utilities"

export default {
    ...(searchClassNameModules(
        "privateChannelsHeaderContainer-1UWASm"
    ) as Object),
} as {
    empty: string
    headerText: string
    privateChannelRecipientsInviteButtonIcon: string
    privateChannelsHeaderContainer: string
    scroller: string
}